-- Creates the database used by the Trade Journal app.
-- Run this once from the backend folder:
--   mysql -u root -p < database.sql
-- (or run the CREATE DATABASE line inside MySQL Workbench)
CREATE DATABASE IF NOT EXISTS trade_journal CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
